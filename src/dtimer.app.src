%% -*- erlang -*-
{application, dtimer,
 [
  {description, ""},
  {vsn, "1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
                  riak_core,
                  hackney,
                  jobs,
		  watchbin
                 ]},
  {mod, { dtimer_app, []}},
  {env, [
    { jobs, [
      { queues, [
        { http_requests, [ { max_size, 50000},  {regulators, [{ rate, [{limit, 5000}]}]}]}
      ]}
    ]}
  ]}
 ]}.
